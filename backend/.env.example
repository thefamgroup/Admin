# Database
DATABASE_URL=postgresql://postgres:password@localhost:5432/tfg_admin
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=password
DB_NAME=tfg_admin

# JWT
JWT_SECRET=change-this-to-a-long-random-secret-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=change-this-refresh-secret-too
JWT_REFRESH_EXPIRES_IN=7d

# App
PORT=4000
NODE_ENV=development
FRONTEND_URL=http://localhost:3001

# Admin seed (first run only)
ADMIN_EMAIL=admin@thefamgroup.co.uk
ADMIN_PASSWORD=Admin@123!

# WhatsApp (Twilio or wa.me links for now)
WHATSAPP_NUMBER=07769240184
